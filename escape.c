#include <stdio.h>
#define pi 3.14
int main()
{
    printf("  \\ tab character \t\t\t my backslash \\n \a  %f", pi);
    
    /*
    for \ in output we need \\
    to print \n in output we need \\n
    to get space between a line in output we can use \t
    to get windows sound use \a
    we can make a code coment by selecting unrequired code and prss ctrl+/
    */

    return 0;
}
