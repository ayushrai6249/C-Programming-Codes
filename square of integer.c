#include <stdio.h>

int main()
{
   //learn to use operations with decimal numbers
    int a;
    printf("enter the number a\n");
    scanf("%d", &a);
    printf("the square of integer is %d\n", a*a);
    return 0;
}